const LoginNavBar = ({children, pageProps}) => (
    
    <div id="navbar" className="navbar">
         
        <div className="pure-menu pure-menu-horizontal">
            <span href="#" className="pure-menu-heading">ADMIN LOGIN</span>
        </div>
    </div>
    )
    
export default LoginNavBar;