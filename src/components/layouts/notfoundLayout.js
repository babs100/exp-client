

const notfoundLayout = ({children, pageProps}) => (
<div id="layout" className="layout">
    
    <div id="main" className="main">
        {children}
    </div>

</div>
)

export default notfoundLayout;
